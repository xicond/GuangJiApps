# Server-side implementation notes

## Completed
- Created a Gin-based server scaffold under the Gin project folder.
- Added a basic router with public endpoints: `/ping`, `/login`, `/forgot-password`, and `/reset-password`.
- Added environment-based configuration loading with `.env.example`.
- Added initial unit tests for the router and a Dockerfile for containerized builds.
- Verified the server starts and `/ping` returns `{"message":"pong"}`.
- Added `ChangePassword` method to `AuthService` and registered `/change-password` and `/v1/change-password` endpoints in Gin router.
- Updated `KelasService` `Create` and `Update` methods to follow the `domain.Kelas` GORM model (`T_TRX_KELAS`) and added lookup validations for `KodeKelas` (`B_KELASKHUSUS`) and `KodeFotang` (`B_FOTHANG`).
- Updated `router.go` error handling (`respondError`) to return 404 Not Found for missing records instead of 500.
- Added `GET /v1/kelas/:id/peserta` endpoint with stored procedure `SP_TRX_KELAS_PESERTA_SEARCH_DATA` and pagination (`page`, `limit`).
- Updated Vue frontend `KelasForm`, `KelasEdit`, `KelasView`, and `KelasPesertaTable` with pagination controls and preloaded lookup support.
- Fixed `IntBool` custom type scanning in `legacy_models.go` to support all database column data types (`bool`, `uint8`, `int16`, `int8`, `uint`, `string`, `float`, `[]byte`) and added JSON marshal/unmarshal handling, preventing row scan errors and empty response arrays.
- Updated `KelasService.Peserta` stored procedure invocation to use explicit named parameters (`@PageSize`, `@CurrentPage`, `@SortDirection`, `@TrxId`, `@FotangId`) with numeric `TrxId` parsing for `SP_TRX_KELAS_PESERTA_SEARCH_DATA`.
- Implemented full CRUD operations (`Create`, `Get`, `Update`, `Delete`) in `KelasPesertaService` (`T_TRX_KELAS_PESERTA`), registered router endpoints (`/kelas-peserta`), added unit tests (`kelas_peserta_service_test.go`), and updated Postman collection.
- Updated `Kelas` struct in `legacy_models.go` to add `omitempty` tag (`json:"trx_id,omitempty"`).
- Added GORM relation link (`Kelas *Kelas`) on `TrxId` in all child structs (`KelasPeserta`, `KelasPengabdi`, `KelasTopik`, `KelasKendaraan`, `KelasDonasi`, `KelasDonasiBarang`, `KelasPengeluaran`, `KelasMusik`, `KelasAbsensi`) in `legacy_models.go`.
- Implemented full CRUD services, unit tests, router endpoints, and Postman collections for all 8 Kelas sub-models (`KelasPengabdi`, `KelasTopik`, `KelasKendaraan`, `KelasDonasi`, `KelasDonasiBarang`, `KelasPengeluaran`, `KelasMusik`, `KelasAbsensi`).
- Converted `KelasPesertaTable.vue` to accordion panel layout with lazy fetching (only fetches data when expanded) and added popup dialog mode for creating and editing `Kelas Peserta` records.
- Added accordion table components with lazy fetching and popup dialog CRUD (Add, Edit, Delete) for `KelasPengabdiTable.vue`, `KelasTopikTable.vue`, `KelasDonasiTable.vue`, and `KelasDonasiBarangTable.vue`, integrated into `KelasEdit.vue` and `KelasView.vue`.
- Updated all `Kelas` sub-resource routes to nested `/v1/kelas/:id/<sub-resource>` URL path structure across Gin router (`router.go`), router unit tests (`router_test.go`), Postman collection (`apps-gin.postman_collection.json`), and Vue API client (`vue/src/api/kelas.ts`), and verified with `go test ./...`.
- Added Logistik/presence form fields (`anak`, `suster`, `menginap`, `makanan_pagi`, `makanan_siang`, `makanan_malam`) to `KelasPengabdiTable.vue` dialog form, state, and create/update handlers.
- Fixed stored procedure parameter placeholder count (from 8 to 9 placeholders) and `@LoginId` binding for `SP_TRX_KELAS_SEARCH_DATA` in `KelasService.List`, resolving data filtering truncation issue.
- Consolidated repetitive `T_APP_LOOKUP` queries into a unified `Lookup(db, categoryID, page, limit, filters)` function in `lookup_service.go`, supporting `limit=0` to return all records without pagination, replaced duplicated lookup code across `FotangService`, `KelasService`, and `TimKerjaService` (`Lookup`, `LookupSub`, `LookupReport`), updated `parsePaginationAndFilters` in `router.go` to preserve `limit=0`, and added unit tests in `lookup_service_test.go`, `fotang_service_test.go`, `kelas_service_test.go`, and `tim_kerja_service_test.go`.
- Created all dedicated lookup routes (`/v1/lookup/waktu-ciu-tao`, `/v1/lookup/gender`, `/v1/lookup/tcs`, `/v1/lookup/fotang`, `/v1/lookup/kelas`, `/v1/lookup/pendidikan`, `/v1/lookup/kelas-umum`, `/v1/lookup/pekerjaan`, `/v1/lookup/keluarga`, `/v1/lookup/status`, and dynamic `/v1/lookup/category/:category`) in Gin router (`router.go`), updated Postman collection (`apps-gin.postman_collection.json`), and added unit tests in `lookup_service_test.go` and `router_test.go`.
- Updated `Lookup` function and `LookupService.Lookup` in `lookup_service.go` to support optional `customSubQuery` parameter (accepting either a custom `*gorm.DB` query or a modifier callback function `func(*gorm.DB) *gorm.DB`) to override or append conditions to the default `T_APP_LOOKUPCATEGORY` subquery. Verified with unit tests in `lookup_service_test.go`.
- Registered `/v1/tim-kerja/lookup`, `/v1/tim-kerja/lookup/:id/sub`, and `/v1/tim-kerja/lookup-report` endpoints in Gin router (`router.go`), added Postman collection entries (`apps-gin.postman_collection.json`), and verified with unit tests in `router_test.go` and `tim_kerja_service_test.go`.
- Optimized backend lookup validation in `UmatService` (`umat_service.go`) to execute lookup checks concurrently using goroutines (`sync.WaitGroup`), collecting all field validation errors into an aggregated array before returning, minimizing GC overhead and query latency. Verified with `go test`.
- Standardized HTTP 400 validation error handling across `router.go`, `validator.go`, and `umat_service.go` to return `{ "error": "Validation failed", "details": { "fieldname": ["error message"] } }` structure for struct binding, `ValidateStruct`, and concurrent lookup validation (`validateUmatLookups`). Verified with `go test ./...`.
- Configured `validator.New()` with `RegisterTagNameFunc` in `validator.go` to ensure validation error detail keys (`details map[string][]string`) use the JSON tag names (`nama_indonesia`, `status_umat`, `jenis_kelamin`, etc.) instead of Go struct field names. Verified with `go test`.
- Created reusable `<FieldErrors />` component (FieldErrors.vue) and integrated field error details rendering into UmatForm.vue, UmatCreateList.vue, and UmatEditList.vue to display multiple error rows directly beneath each related input field based on backend `details` JSON map. Verified with `vue-tsc --noEmit`.
- Integrated `<FieldErrors />` component and field validation error handling across all Kelas components (KelasForm.vue, KelasPesertaTable.vue, KelasPengabdiTable.vue, KelasTopikTable.vue, KelasDonasiTable.vue, and KelasDonasiBarangTable.vue) as well as views (KelasCreate.vue and KelasEdit.vue) to display field error messages in multiple rows under input fields and dialog forms based on backend `details`. Verified with `vue-tsc --noEmit`.
- Updated `UmatForm.vue` with Tim Kerja lookup select (`getLookupTimKerja`), Pekerjaan lookup select (`getLookupPekerjaan`), Ikrar 1-6 checkboxes (`ikrar_1`..`ikrar_6`), status checkboxes and paired DatePickers (`ren_chai_pan`, `lien_ciang_pan`, `ciang_yen_pan`, `ching_khou`, `meninggal`), `nama_tcs_lain`, `nama_fotang_lain`, and numeric `uang_pahala`. Verified with `vue-tsc --noEmit` and `go test ./...`.
- Registered `GET /v1/topics` endpoint in Gin router (`router.go`), added `getTopikLookup` method in `topic.ts`, and updated `KelasTopikTable.vue` to use `<LookupSelect>` for Nama Topik lookup, displaying the selected Topic's Kategori (`topic_category`) and Keterangan (`description`) as static text in `el-form-item`. Verified with `vue-tsc --noEmit` and `go test ./...`.
- Updated `KelasForm.vue` to use `<LookupSelect>` with `lookupApi.getLookupKelasLevel` (`/v1/lookup/kelas-level`) for "Tingkat / Level". Added `page`, `limit`, and `lookup_description` query parameters across all 17 lookup endpoints in [apps-gin.postman_collection.json](file:///Users/xicond/Workspace/www/GuangJiApps/gin/postman/apps-gin.postman_collection.json). Verified with `vue-tsc --noEmit` and `go test ./...`.
- Updated `KelasPengabdiTable.vue` to render Data Logistik / Kehadiran as a dynamic per-day table multiplied by the class duration (`Tanggal Selesai` - `Tanggal Mulai`), supporting numeric inputs for `anak` & `suster` and checkboxes for `menginap`, `makanan_pagi`, `makanan_siang`, & `makanan_malam`. Form values are parsed from comma-separated strings on load and saved as comma-separated values on submit. Verified with `vue-tsc --noEmit` and `go test ./...`.
- Updated `KelasPesertaTable.vue` to render Data Logistik / Kehadiran as a dynamic per-day table multiplied by the class duration (`Tanggal Selesai` - `Tanggal Mulai`), supporting numeric inputs for `anak` & `suster` and checkboxes for `menginap`, `makanan_pagi`, `makanan_siang`, & `makanan_malam`. Form values are parsed from comma-separated strings on load and saved as comma-separated values on submit. Verified with `vue-tsc --noEmit` and `go test ./...`.
- Updated `KelasForm.vue` to add proper padding to `.mc-collapse` header (`0.75rem 1rem`) and body (`1.25rem 1.25rem 0.5rem 1.25rem`) for the Pembawa Acara / MC collapsible section. Verified with `vue-tsc --noEmit`.
- Updated `StartDate`, `EndDate`, and `Deadline` fields in `Kelas` struct ([legacy_models.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/domain/legacy_models.go#L638)) from `*time.Time` to `*DateOnly` so that GET `/v1/kelas/:id` returns dates in ISO `YYYY-MM-DD` format and POST/PATCH `/v1/kelas` handles date strings (`2019-04-14T00:00:00Z`, `2019-04-14`, or empty `""` / `null`) cleanly without JSON parsing errors. Registered `DateOnly` in `validator.go` for date range comparison. Verified with `vue-tsc --noEmit` and `go test ./...`.
- Added `FieldTranslations` dictionary mapping and `GetFieldLabel` / `FormatFieldName` helper functions in `legacy_models.go` to translate struct field names (e.g. `Umat.NamaIndonesia` into `Nama Indonesia`), falling back to space-separated PascalCase/snake_case formatting by default, and updated `FormatValidatorErrors` in `validator.go` to present user-friendly error messages while preserving original detail keys for frontend form binding. Verified with unit tests in `legacy_models_test.go` and `validator_test.go`.
- Refactored `validateKelasLookups` in `kelas_service.go` to validate lookup fields (`kode_kelas`, `kode_fotang`, `level`) concurrently using `sync.WaitGroup` and return `&ValidationError{Details: details}` matching `validateUmatLookups` in `umat_service.go`. Updated `KelasService.Update` to use `validateKelasLookups`. Verified with `TestKelasService`.
- Added `validatePesertaLookups` (`trx_id` from `Kelas`, `id_peserta` from `Umat`, `tim_kerja` from `B_TIMKERJA`), `validatePengabdiLookups` (`trx_id` from `Kelas`, `id_pengabdi` from `Umat`, `tim_kerja` from `B_TIMKERJA`, `sub_kerja` from `B_SUBKERJA`), and `validateTopikLookups` (`trx_id` from `Kelas`, `kode_topik` from `Topic`, `penceramah` from `Umat`) across `kelas_peserta_service.go`, `kelas_pengabdi_service.go`, and `kelas_topik_service.go`, returning structured `&ValidationError{Details: details}`. Verified with `go test ./...`.
- Updated `KelasForm.vue` to use a unified `el-date-picker` `type="daterange"` component bound via a computed property to `formData.start_date` and `formData.end_date`. Added `validate:"omitempty,gtefield=StartDate"` to `EndDate` in `legacy_models.go` and `gtefield`/`ltefield` error message translations in `validator.go` to enforce `Kelas.StartDate <= Kelas.EndDate`. Verified with `vue-tsc --noEmit` and `go test ./...`.
- Implemented `KelasService.Report` method and Gin endpoints (`GET /v1/kelas/:id/report`) to stream SSRS Excel reports (`rpt_trx_kelas`) with zero-copy buffer pooling (`sync.Pool`), configurable base URL (`REPORT_SERVER_URL`), username & password authentication (`REPORT_SERVER_USERNAME`, `REPORT_SERVER_PASSWORD`), HTTP Basic Auth, and HTTP Digest Auth challenge retry. Retrieved default `SubWhId` automatically from `T_WH_USER_MATRIX_MST` (`AdminMatrix`) linked to logged in `userID`. Installed `excelize/v2`, added Postman entry, and added unit tests in `kelas_service_test.go`. Verified with `go test ./...`.
- Added `downloadReport` method to `kelasApi` (`vue/src/api/kelas.ts`) and added a green Download action button (`Download` icon) with `ElLoading.service` full-page overlay loading, automatic Blob JSON error parsing (`err.response.data.text()`), and cleanup handling on success/failure in `KelasList.vue` (`vue/src/views/transaction/KelasList.vue`). Verified with `npx vue-tsc --noEmit`.
- Added search by `lookup_value` and multi-field keyword search (`LookupDescription`, `LookupValue`, `LookupId`) across `Lookup` in `lookup_service.go`, `FotangService.Lookup`, `KelasService.Lookup`, `TimKerjaService.Lookup`, `TimKerjaService.LookupSub`, `TimKerjaService.LookupReport`, and `TimKerjaService.List` in `tim_kerja_service.go`. Updated all 17 lookup requests in `apps-gin.postman_collection.json` with `lookup_value` and `search` query parameters, added unit tests in `lookup_service_test.go`, and verified with `go test ./...`.
- Exported `lookup_value`, `lookup_id`, and `search` query parameters in `types/lookup.ts` ([lookup.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/types/lookup.ts)) and updated API methods in `api/fotang.ts` ([fotang.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/api/fotang.ts)), `api/kelas.ts` ([kelas.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/api/kelas.ts)), and `api/lookup.ts` ([lookup.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/api/lookup.ts)). Added `checkAndFetchMissingSelectedValue()` in `LookupSelect.vue` ([LookupSelect.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/components/common/LookupSelect.vue)) to auto-fetch missing selected options by `lookup_value`/`lookup_id` when loading edit forms where the selected item is on page 2+. Verified with `npx vue-tsc --noEmit`.
- Implemented paginated `Report` (`SP_SXY_RPT_TRANSAKSI`) returning `SxyDonasiReport` & total summary, and `ReportExcel` SSRS Excel streaming in `donasi_sxy_service.go` ([donasi_sxy_service.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/donasi_sxy_service.go)). Registered `GET /v1/donasi-sxy/report` and `GET /v1/donasi-sxy/report/excel` endpoints in `router.go` ([router.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/api/router.go)) and added requests to `apps-gin.postman_collection.json` ([apps-gin.postman_collection.json](file:///Users/xicond/Workspace/www/GuangJiApps/gin/postman/apps-gin.postman_collection.json)). Updated `SxyReportList.vue` ([SxyReportList.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/views/report/SxyReportList.vue)) with filters (Donatur, Penggalang, Fotang `LookupSelect`, DateRange), data table pagination, table footer summary (`show-summary` & `:summary-method="getSummaries"`) right-aligned with the `Jumlah` column, total summary banner, and conditional Excel report download (`ReportExcel`) enabled when rows exist. Verified with `go test ./...` and `npx vue-tsc --noEmit`.
- Implemented PEM file key loading for `JWT_SECRET` and `REFRESH_SECRET` in `config.go` ([config.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/config/config.go)). `GetJWTSigningKey()` and `GetJWTVerificationKey()` automatically detect and load OpenSSL RSA (`*rsa.PrivateKey` / `*rsa.PublicKey`), EC (`*ecdsa.PrivateKey` / `*ecdsa.PublicKey`), or HMAC keys from `.pem` file paths or raw secret strings. Updated `AuthMiddleware` ([auth.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/api/middleware/auth.go)) and `GenerateToken` ([auth_service.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/auth_service.go)) to resolve verification and signing keys via `config`. Added unit tests in `config_test.go` ([config_test.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/config/config_test.go)) and verified with `docker compose run --rm gin-build` and `go test ./...`.
- Added `LookupKategoriTopic` route `GET /v1/lookup/kategori-topic` (CategoryId `B_KATEGORI_TOPIK`) in `router.go` ([router.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/api/router.go)) and Postman collection `apps-gin.postman_collection.json` ([apps-gin.postman_collection.json](file:///Users/xicond/Workspace/www/GuangJiApps/gin/postman/apps-gin.postman_collection.json)). Added `getLookupKategoriTopic` method in `api/lookup.ts` ([lookup.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/api/lookup.ts)) and implemented complete CRUD modal dialog, filter section, and `LookupSelect` integration for `TopicCategory` in `TopicList.vue` ([TopicList.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/views/master-data/TopicList.vue)). Verified with `router_test.go` and `npx vue-tsc --noEmit`.
- Added `LookupKategoriEvent` route `GET /v1/lookup/kategori-event` (CategoryId `B_KATEGORI_EVENT`) in `router.go` ([router.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/api/router.go)) and Postman collection `apps-gin.postman_collection.json` ([apps-gin.postman_collection.json](file:///Users/xicond/Workspace/www/GuangJiApps/gin/postman/apps-gin.postman_collection.json)). Added `getLookupKategoriEvent` method in `api/lookup.ts` ([lookup.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/api/lookup.ts)) and implemented complete CRUD modal dialog, filter section, and `LookupSelect` integration for `EventCategory` in `ActivityList.vue` ([ActivityList.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/views/master-data/ActivityList.vue)). Added `EventCategoryInfo` to `Activity` struct in `legacy_models.go` ([legacy_models.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/domain/legacy_models.go)) and `types/activity.ts` ([activity.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/types/activity.ts)). Verified with `docker compose run --rm gin-build` and `npx vue-tsc --noEmit`.
- Implemented complete CRUD modal dialogs, form validation, submit loading states (`submitting`), and Fotang lookup (`/fotang/lookup` via `fotangApi.getFotangLookup` & `fotangApi.getFotangLookupItem` with `LookupSelect`) across `TahunCiuTaoList.vue` ([TahunCiuTaoList.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/views/master-data/TahunCiuTaoList.vue)), `PenggalangDanaList.vue` ([PenggalangDanaList.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/views/master-data/PenggalangDanaList.vue)), and `SxyDonaturList.vue` ([SxyDonaturList.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/views/master-data/SxyDonaturList.vue)). Added `getFotangLookupItem` in `fotang.ts` ([fotang.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/api/fotang.ts)) for single Fotang resolution on edit/filter. Verified with `npx vue-tsc --noEmit`.
- Refactored single-resource Topic endpoints from `/v1/topics/:id` to query param `/v1/topic?code=...` for `GET`, `PATCH`, `DELETE` and `/v1/topic` for `POST` in `router.go` ([router.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/api/router.go)), `api/topic.ts` ([topic.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/api/topic.ts)), and Postman collection `apps-gin.postman_collection.json` ([apps-gin.postman_collection.json](file:///Users/xicond/Workspace/www/GuangJiApps/gin/postman/apps-gin.postman_collection.json)) to support topic codes cleanly without path parameter conflicts. Verified with `docker compose run --rm gin-build` and `npx vue-tsc --noEmit`.
- Added `LookupTipeSumbangan` route `GET /v1/lookup/tipe-sumbangan` (CategoryId `SXY_TIPESUMBANGAN`) in `lookup_service.go` ([lookup_service.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/lookup_service.go)), `router.go` ([router.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/api/router.go)), and Postman collection `apps-gin.postman_collection.json` ([apps-gin.postman_collection.json](file:///Users/xicond/Workspace/www/GuangJiApps/gin/postman/apps-gin.postman_collection.json)). Added `getLookupTipeSumbangan` in `api/lookup.ts` ([lookup.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/api/lookup.ts)) and implemented complete CRUD modal dialog with Sxy Donatur lookup (`sxyDonaturApi.getSxyDonaturs` & `sxyDonaturApi.getSxyDonaturById`), Tipe Sumbangan lookup (`lookupApi.getLookupTipeSumbangan`), and Penggalang Dana lookup (`penggalangDanaApi.getPenggalangDanas`) in `DonasiSxyList.vue` ([DonasiSxyList.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/views/transaction/DonasiSxyList.vue)). Updated `DonasiSxy` interface in `types/donasiSxy.ts` ([donasiSxy.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/types/donasiSxy.ts)). Verified with `docker compose run --rm gin-build` and `npx vue-tsc --noEmit`.
- Refactored `LookupSelect.vue` ([LookupSelect.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/components/common/LookupSelect.vue)) with flexible prop type signatures (`fetchApi: (params: any, signal?: AbortSignal) => Promise<{ data?: any[]; meta?: any }>`) and generic option handling. Fixed type compatibility across all Vue components (`UmatForm.vue`, `DonasiSxyList.vue`, `SxyReportList.vue`, `TopicList.vue`, etc.). Verified with `npx vue-tsc --noEmit`.
- Created custom `DateTime` type (`type DateTime struct { time.Time }`) in `legacy_models.go` ([legacy_models.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/domain/legacy_models.go)) to replace `time.Time` on date-time model fields across `legacy_models.go`. Implemented `Scan`, `Value`, `UnmarshalJSON` (supporting ISO/RFC3339 date strings like `2022-10-08T09:12:39.423Z`), and `MarshalJSON` (formatting timestamps to `YYYY-MM-DD HH:mm:ss`, e.g., `2022-10-08 09:12:39`). Registered `DateTime` in `validator.go` ([validator.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/validator.go)), updated service assignments across `internal/service`, added unit tests (`TestDateTimeJSON`, `TestDateTimeScan`) in `legacy_models_test.go`, and verified with `go test ./internal/domain/...` and `go build ./...`.
- 58: Refactored `StatusNotModifiedHeader` middleware (`statusnotmodified.go`) and `LoginRateLimiter` (`ratelimit.go`) to use a thread-safe Singleton `*redis.Client` instance (`database.GetRedisClient(cfg)` via `sync.Once`), eliminating duplicate Redis client instantiations and redundant connection pools. Added HTTP headers `Cache-Control: public, max-age=604800, must-revalidate` (1 week browser/CDN cache), `Last-Modified` (formatted via `http.TimeFormat`), `ETag`, and `If-Modified-Since` request header validation. Attached the ETag caching middleware with 30s TTL in `router.go` across GET list routes (`/umats`, `/topics`, `/activities`, `/tim-kerja`, `/tahun-ciu-tao/list`, `/penggalang-dana`, `/sxy-donatur`) and all lookup routes (`/fotang/lookup`, `/fotang/lookup-sxy`, `/tim-kerja/lookup`, `/tim-kerja/lookup-report`, `/kelas/lookup`, `/lookup/waktu-ciu-tao`, `/lookup/gender`, `/lookup/tcs`, `/lookup/kelas-level`, `/lookup/fotang`, `/lookup/kelas`, `/lookup/pendidikan`, `/lookup/kelas-umum`, `/lookup/pekerjaan`, `/lookup/keluarga`, `/lookup/status`, `/lookup/kategori-topic`, `/lookup/kategori-event`, `/lookup/tipe-sumbangan`), reducing table reads and SQL DB load. Added unit tests in `statusnotmodified_test.go` and verified with `go test ./internal/api/middleware/...`.
- 59: Added `UmatReport` struct in `legacy_models.go` ([legacy_models.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/domain/legacy_models.go)) and implemented `Report` and `ReportExcel` methods in `UmatService` ([umat_service.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/umat_service.go)) executing stored procedure `SP_BUS_RPT_UMAT` with parameters (`FotangAktif`, `FotangChiuTao`, `NamaMandarin`, `StartDate`, `EndDate`, `NamaIndo`, `Pengajak`, `Alias`, `UsiaDari`, `UsiaSampai`, `IsLulusSd`, `IsVege`, `StatusUmat`). Created universal `nullFieldScanner` in `umat_service.go` to handle NULL database column scans safely across all primitive types (`int32`, `float64`, `bool`, `DateOnly`), resolving row truncation issues where NULL columns previously caused `rows.Scan` errors and skipped valid result rows. Mounted HTTP routes `GET /v1/umats/report` and `GET /v1/umats/report/excel` in `router.go` ([router.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/api/router.go)) with streaming SSRS Excel report `rpt_bus_umat_list`. Updated Postman collection `apps-gin.postman_collection.json` ([apps-gin.postman_collection.json](file:///Users/xicond/Workspace/www/GuangJiApps/gin/postman/apps-gin.postman_collection.json)) and verified with `go build ./...`.
- 60: Implemented `UmatReportList.vue` view component ([UmatReportList.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/views/report/UmatReportList.vue)), `UmatReportItem` / `UmatReportQueryParams` types in `umat.ts` ([umat.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/types/umat.ts)), and `getUmatReport` / `downloadUmatReportExcel` methods in `api/umat.ts` ([umat.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/api/umat.ts)). Added route `/report/master/umat` in `router/index.ts` ([index.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/router/index.ts)) with complete filter controls (Fotang Aktif, Fotang Chiu Tao, Status Umat, Nama Indonesia, Nama Mandarin, Pengajak, Alias, Rentang Tanggal Chiu Tao, Usia, Lulus SD3, Vege), pagination, and Excel download functionality without summary total footer card. Verified with `npx vue-tsc --noEmit`.
- 62: Upgraded `github.com/quic-go/quic-go` to `v0.59.1` in `go.mod`, resolving third-party module vulnerability GO-2026-5676. Evaluated standard library vulnerabilities via `govulncheck ./...`, confirming HTTP report calls are restricted to localhost and OCR API calls to trusted services, categorizing standard library parsing/TLS findings as non-exploitable false positives.
- 63: Updated `LookupSelect.vue` ([LookupSelect.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/components/common/LookupSelect.vue)) to perform local searching by `labelKey` (and `valueKey`) on client-side state without triggering unnecessary remote API requests when `autoPopulate: true` is enabled and the initial un-queried dataset fits within a single page (`total <= pageSize`). Verified with `npx vue-tsc --noEmit` and `go build ./...`.
- 64: Restricted `topik_date` picker in `KelasTopikTable.vue` ([KelasTopikTable.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/components/kelas/KelasTopikTable.vue)) strictly to the `date_range` (`start_date` and `end_date`) of the parent `KelasForm`. Added `:disabled-date` calendar disabling, range validation in `formRules`, and smart default date selection when opening the Add Topic dialog. Passed `startDate` and `endDate` from `KelasEdit.vue` ([KelasEdit.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/views/transaction/KelasEdit.vue)) and `KelasView.vue` ([KelasView.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/views/transaction/KelasView.vue)). Verified with `npx vue-tsc --noEmit`.
- 65: Removed HMAC (`HS256`) fallback in `config.go` ([config.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/config/config.go)) and enforced RSA (`RS256`) / ECDSA (`ES256`) PEM keys exclusively. Added candidate path resolution in `getSecretBytes` to seamlessly resolve container paths (`/app/certs/dev-private-key.pem`) to local relative paths (`certs/dev-private-key.pem`) during local execution. Configured `auth.go` ([auth.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/api/middleware/auth.go)) `jwt.WithValidMethods` for `RS256` and `ES256` only. Verified with `go test ./internal/config/...` and `go test ./internal/api/middleware/...`.
- 66: Changed placeholder in `index.html` ([index.html](file:///Users/xicond/Workspace/www/GuangJiApps/vue/index.html)) from `%VITE_DNS_PREFETCH_TAGS%` to `<!-- VITE_DNS_PREFETCH_TAGS -->` and updated `dnsPrefetchPlugin` in `vite.config.ts` ([vite.config.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/vite.config.ts)), eliminating Vite's built-in env substitution warning during `npm run build:client`. Verified with clean `npm run build:client` build.
- 67: Enhanced OCR Recognition overlay & date parsing: Added `ElLoading` full-screen service overlay during OCR image upload in `UmatCreateList.vue` ([UmatCreateList.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/views/master-data/UmatCreateList.vue)). Restored `props.initialData` watcher merging into `formData.value` in `UmatForm.vue` ([UmatForm.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/components/umat/UmatForm.vue)) and mapped gender strings ("WANITA" -> "002", "PRIA" -> "001") in `applyOcrToForm` so form inputs and `LookupSelect` components populate seamlessly when applying OCR data. Updated `ParseUmatOcrText` and added `parseGregorianDateFromText` in `umat_service.go` ([umat_service.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/umat_service.go)) to parse `tanggal_chiutao_int` (supporting formats like `9/8′26` -> `2026-08-09` and `22 Aug 26` -> `2026-08-22`), trailing duplicated Chinese character deduplication in `deduplicateName`, and flexible `PERANTARA` / `引師` matching without requiring colons (e.g. `PERANTARA Felx` -> `pengajak_manual: Felx`). Added test assertions in `umat_service_test.go` and verified with `go test -v ./internal/service -run ^TestParseUmatOcrText$` (**PASS**) and `npx vue-tsc --noEmit` (**PASS**).
- 68: Enhanced OCR Pendidikan parsing: Updated `ParseUmatOcrText` in `umat_service.go` ([umat_service.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/umat_service.go)) to normalize case-insensitive variations and OCR misreadings of education levels (`Si`, `SI`, `S i`, `S`, `S-1`, `S/1`) into `"S1"`, as well as `SII` -> `"S2"` and `SIII` -> `"S3"`. Added unit test assertions in `umat_service_test.go` ([umat_service_test.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/umat_service_test.go)) and verified with `go test -v ./internal/service -run ^TestParseUmatOcrText$` (**PASS**).
- 69: Added `KelasKendaraanTable.vue` ([KelasKendaraanTable.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/components/kelas/KelasKendaraanTable.vue)) and `KelasPengeluaranTable.vue` ([KelasPengeluaranTable.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/components/kelas/KelasPengeluaranTable.vue)) collapsible accordion table components with lazy fetching, pagination, and modal CRUD dialogs with form validation (`FieldErrors`), multi-day boolean kehadiran schedule matching `KelasPesertaTable` & `KelasPengabdiTable` (`daysCount` calculated from `start_date` and `end_date`), and lookup select (`getLookupTimKerja`). Defined `KelasKendaraan` and `KelasPengeluaran` interfaces in `types/kelas.ts` ([kelas.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/types/kelas.ts)), added CRUD API methods in `api/kelas.ts` ([kelas.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/api/kelas.ts)), and integrated both components into `KelasEdit.vue` ([KelasEdit.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/views/transaction/KelasEdit.vue)) and `KelasView.vue` ([KelasView.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/views/transaction/KelasView.vue)). Verified with `npm run build:client` (**PASS**).
- 70: Optimized `KelasKendaraanService.Create` in `kelas_kendaraan_service.go` ([kelas_kendaraan_service.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/kelas_kendaraan_service.go)) to execute `SP_APP_GenerateId` ID generation and `AdminMatrix.SUBWHID` resolution concurrently using `sync.WaitGroup`, reducing database latency on record creation. Verified with `go build ./...` (**PASS**).
- 71: Refactored Fail2Ban 404 IP blocking middleware in `fail2ban.go` ([fail2ban.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/api/middleware/fail2ban.go)) to re-read existing blocked remote IPs directly from Windows Firewall (`ReadBlockedIPsFromNetsh` via `netsh advfirewall firewall show rule name=Golang_Block_0`), eliminating persistent slice storage in Go memory for minimal GC pressure. Updated `f.Middleware()` to trigger `go f.Record404(ip, reqPath)` as a non-blocking "fire-and-forget" goroutine so HTTP 404 client responses return immediately without latency. Verified with `CGO_ENABLED=0 go test -v -run "^(TestIsScannerURL|TestFail2Ban)" ./internal/api/middleware/...` (**PASS**) and `go build ./...` (**PASS**).
- 72: Implemented conditional display of "Ikrar Yg Dipilih" checkboxes in `KelasPesertaTable.vue` for `KodeKelas == "004"` on Edit and View modes. Added `Umat` relation (`Umat *Umat`) to `KelasPeserta` model in `legacy_models.go` with flexible JSON unmarshaling for `ikrar_1`..`ikrar_6`. Updated `KelasPesertaService` (`Create`, `Get`, `Update`) in `kelas_peserta_service.go` to save and update nested `Umat` ikrar data in `T_BUS_UMAT` with `KodeKelas == "004"` validation and atomic transaction. Updated `KelasPesertaTable.vue` dialog form to select and load Umat ikrars dynamically and submit nested `umat` payload. Updated Postman collection and added unit test in `kelas_peserta_service_test.go`. Verified with `vue-tsc --noEmit` and `go build ./...`.
- 73: Added uniqueness validation for `topic_name` combined with `ValidateStruct` in `TopicService` ([topic_service.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/topic_service.go)). Implemented `validateTopic` which executes struct validation and `validateTopicName` together, collecting and returning all field validation errors combined in a single `ValidationError`. On `Create`, validates struct tags and rejects duplicates among active topics (`Status = true`). On `Update`, verifies whether `topic_name` is unchanged; if unchanged, duplicate validation passes for its own record, and if changed, verifies no other active topic (`TopicCode <> id`) has the new `topic_name`. Added comprehensive unit tests in `topic_service_test.go` verifying combined multi-field validation error delivery and verified with `go test -v ./internal/service -run TestTopicService` and `docker compose run --rm gin-build`.
- 74: Enhanced `TopicList.vue` ([TopicList.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/views/master-data/TopicList.vue)) with auto-suggest on `topic_name` in create dialog (`el-autocomplete`) triggered when entering 2 or more words (`split(/\s+/).filter(Boolean).length >= 2`). Selecting an item from suggestions automatically switches the dialog into edit mode for that topic (`topic_code`). On submit, if backend reports duplicate name error (`topic_name` contains `"sudah ada"`), automatically resolves the existing topic and renders an inline link `", update here instead"` under `topic_name` that opens the edit dialog for that existing topic. Disabled dialog form during submission (`:disabled="submitting"`). Verified with `vue-tsc --noEmit` and `npm run build:client`.
- 75: Resolved `validateTopic` validation combining and error clue delivery:
  - In `router.go` ([router.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/api/router.go)), replaced `c.ShouldBindJSON` with `json.NewDecoder` decoding in `POST /topic` and `PATCH /topic` so Gin's binding does not prematurely short-circuit on struct validation tags before `validateTopic` runs, enabling combined delivery of both `ValidateStruct` and `validateTopicName` errors together.
  - In `respondValidationError` ([router.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/api/router.go)), replaced static `"error": "Invalid Input"` with `errMsg := err.Error()`, ensuring human-readable error clues are always returned in HTTP 400 responses alongside the `details` map.
  - In `topic_service.go` ([topic_service.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/topic_service.go)), updated `Update` to preserve `item.TopicName` and `item.TopicCategory` when omitted in PATCH payloads, and updated `validateTopic` error aggregation.
  - In `gin/dist/web.config` ([web.config](file:///Users/xicond/Workspace/www/GuangJiApps/gin/dist/web.config)), changed `<httpErrors existingResponse="Replace">` to `<httpErrors existingResponse="PassThrough">` so IIS does not intercept and strip JSON error bodies for HTTP 400 Bad Request or other application-level errors.
- 76: Implemented `KelasMasterService` in `kelas_master_service.go` ([kelas_master_service.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/kelas_master_service.go)) managing `T_APP_LOOKUP` for `CategoryId = 'B_KELASKHUSUS'`:
  - Added automated sequential generation of `LookupValue` (e.g. `023`, `024`) and `LookupId` (`B_KELASKHUSUS023`) based on existing maximum numeric values in `T_APP_LOOKUP`.
  - Added `validateDescription` and `validateKelasMaster` combining `ValidateStruct` and duplicate `LookupDescription` validation within active records (`Status = true`).
  - Implemented `List` with pagination and concurrent GORM queries (`sync.WaitGroup`), `Get`, `Create`, `Update`, and `Delete` (soft delete with `Status = false`).
  - Added comprehensive unit tests in `kelas_master_service_test.go` ([kelas_master_service_test.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/kelas_master_service_test.go)) and verified passing.
  - Registered `Kelas Master` routes in `router.go` ([router.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/api/router.go)): `GET /v1/kelas-masters`, `GET /v1/kelas-master`, `GET /v1/kelas-masters/:id`, `POST /v1/kelas-master`, `PATCH /v1/kelas-master`, `DELETE /v1/kelas-master`, and Redis cache invalidation on mutation.
  - Updated Postman collection `apps-gin.postman_collection.json` ([apps-gin.postman_collection.json](file:///Users/xicond/Workspace/www/GuangJiApps/gin/postman/apps-gin.postman_collection.json)) with 5 CRUD endpoints for Kelas Master.
  - Created Vue client: TypeScript types in `kelasMaster.ts` ([kelasMaster.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/types/kelasMaster.ts)), API service in `kelasMaster.ts` ([kelasMaster.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/api/kelasMaster.ts)), and full CRUD view in `KelasMasterList.vue` ([KelasMasterList.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/views/master-data/KelasMasterList.vue)) with auto-suggest on `LookupDescription` (min 2 words), inline `update here instead` link on duplicate, automated `LookupValue`, responsive design, and submission state locking (`submitting`).
  - Added route `/master-data/kelas` in `router/index.ts` ([index.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/router/index.ts)) with `activeMenu: '/master-data/kelas'`.
  - Added menu item in `MainLayout.vue` ([MainLayout.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/layouts/MainLayout.vue)) under Master Data submenu (`v-if="hasSubMenu('Master Data', 'Kelas')" index="/master-data/kelas"`), ensuring clean menu isolation from `/transaction/kelas`. Added fallback in `authStore.hasSubMenu` ([auth.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/stores/auth.ts)) to allow access if the user has `Master Data` even if `T_Login_Menu` does not yet contain `Kelas`, preventing `router.beforeEach` from intercepting and cancelling navigation. Verified with `npm run type-check` (**PASS**) and `gin-build` (**PASS**).
- 89: - Resolved `@FIXME` in `KelasPesertaService` ([kelas_peserta_service.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/kelas_peserta_service.go)): Implemented `updateUmatSDPemula` helper function to handle SD Pemula (004) participant graduation (`lulus`). Safely retrieves parent `Kelas` record for `tanggalsd3` (`EndDate`) and `tempatsd3` (`KodeFotang`). Automatically updates `Umat.StatusUmat` to `"001"` (Pengabdi) when passing and reverts to `"006"` (Umat Baru) when failing (checking across all active 004 class records for the participant). Soft delete in `Delete` also re-evaluates SD Pemula graduation state. Added unit tests in `kelas_peserta_service_test.go`.
- 90: Resolved empty string validation error on optional email fields (`Umat.Email` / `Admin.Email`): Registered a custom `email` validator in `validator.go` ([validator.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/validator.go)) that allows empty strings (`""`) so `validate:"omitempty,max=50,email"` passes when email is omitted or left empty. Exported `ConfigureValidator` and hooked it into Gin's global `binding.Validator.Engine()` in `router.go` ([router.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/api/router.go)) so that `c.ShouldBindJSON` across all endpoints shares the identical validation rules and custom tag mappings. Added email sanitization (converting `""` to `nil`) in `UmatService.Create` and `UmatService.Update` ([umat_service.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/umat_service.go)). Verified with unit tests in `validator_test.go` and clean `go build ./...`.
- 91: Fixed deadlock and refactored goroutine synchronization in `UmatService` (`Create` and `Update`) ([umat_service.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/umat_service.go)): Replaced fragile manual `tasksCount` tracking with idiomatic `wg.Add(1)` called directly before each spawned goroutine. This completely eliminates any risk of count drift or deadlock in both `Create` and `Update`. Verified with unit tests in `umat_service_test.go` and recompiled via `docker compose run --rm gin-build`.
- 92: Refactored all occurrences of `sync.WaitGroup` throughout the Go codebase (`gin/internal/service`) to Go 1.25's built-in `wg.Go(func() { ... })`. This completely eliminated manual `wg.Add(...)` counting and `defer wg.Done()` calls across all services (`umat_service.go`, `activity_service.go`, `admin_service.go`, `admin_group_service.go`, `admin_sub_warehouse_service.go`, `auth_service.go`, `group_menu_service.go`, `kelas_kendaraan_service.go`, `kelas_master_service.go`, `kelas_pengabdi_service.go`, `kelas_peserta_service.go`, `kelas_service.go`, `kelas_topik_service.go`, `lookup_service.go`, `tim_kerja_service.go`, `topic_service.go`), preventing goroutine counter mismatches, race conditions, or negative counter panics. Verified with `go test ./...`, `go build ./...`, and `docker compose run --rm gin-build`.
- 93: Implemented complete User Acceptance Tests (UAT) and mock sample outputs across all 117 endpoints in Postman collection ([apps-gin.postman_collection.json](file:///Users/xicond/Workspace/www/GuangJiApps/gin/postman/apps-gin.postman_collection.json)):
  - Updated Login endpoint credentials to `admin` / `password` and added automated test script to extract `token` and persist it into collection variable `authToken`.
  - Added comprehensive UAT test scripts (`pm.test`) to all 117 endpoints (validating HTTP status codes, latency < 2000-3000ms, JSON body structures, pagination metadata, and Excel file headers).
  - Generated realistic sample responses (Saved Examples): HTTP 200 OK for all GET endpoints, and dual HTTP 200/201 (Success) + HTTP 400 (Bad Request validation errors) for all POST, PATCH, and DELETE endpoints.
- 94: Implemented complete End-to-End (E2E) User Acceptance Tests in Go (`gin/tests/e2e/`) covering all API endpoints using `httpexpect/v2` and `testify`:
  - Created modular test infrastructure with automated authentication (`admin`/`password`), Bearer token caching, and fake factory payload generators in `gin/tests/e2e/factory/` for Admin, Master Data, and Transaction domains.
  - Implemented comprehensive E2E test suites:
    - `auth_e2e_test.go`: Tests Ping (`/ping`), Login (`POST /login` with 200, 400 bad JSON, 401 invalid credentials), and Change Password (`PATCH /change-password` with 400).
    - `lookup_e2e_test.go`: Tests all 14 lookup endpoints for HTTP 200 and unauthenticated requests for HTTP 401.
    - `admin_mgmt_e2e_test.go`: Tests Department (`GET /v1/departments`), Admins CRUD (`/v1/admins`), Admin Groups CRUD (`/v1/admin-groups`), Group Menu Mappings CRUD (`/v1/group-menus`), and Admin Sub Warehouses CRUD (`/v1/admin-sub-warehouses`).
    - `master_data_e2e_test.go`: Tests Umat CRUD & Reports (`/v1/umats`), Topics CRUD (`/v1/topic`), Kelas Master CRUD (`/v1/kelas-master`), Activity CRUD (`/v1/activities`), Tim Kerja CRUD & Lookups (`/v1/tim-kerja`), Tahun Ciu Tao CRUD (`/v1/tahun-ciu-tao`), Penggalang Dana CRUD (`/v1/penggalang-dana`), SXY Donatur CRUD (`/v1/sxy-donatur`), and Fotang Lookups (`/v1/fotang/lookup`).
    - `transactions_e2e_test.go`: Tests Kelas CRUD (`/v1/kelas`), nested sub-resources (Peserta, Pengabdi, Topik, Kendaraan, Donasi, Donasi Barang, Pengeluaran, Musik, Absensi), SSRS Report generation, and Donasi SXY CRUD (`/v1/donasi-sxy`) with SSRS Excel export (`/v1/donasi-sxy/report/excel`).
  - Tested all GET and DELETE endpoints for HTTP 200, and tested all POST and PATCH endpoints with fake factory data for both HTTP 200/201 (success) and HTTP 400 (validation failure).
  - Fixed backend issues identified during test implementation:
    - Fixed SQL Server `datetimeoffset` out-of-range conversion error on zero dates by updating `DateOnly.Value()` and `DateTime.Value()` in `legacy_models.go` to return formatted strings or `nil`.
    - Added `s.db.Omit("AdminGroup", "Department")` and partial field updating in `AdminService` to prevent foreign key violations on `T_Login_Group`.
    - Fixed stored procedure parameter counts in `UmatService.Report` (`SP_BUS_RPT_UMAT` - 13 parameters) and `DonasiSxyService.Report` (`SP_SXY_RPT_TRANSAKSI` - 5 parameters).
    - Added dual path and query parameter handling in `router.go` for `tahun-ciu-tao`, `activity`, and string lookup IDs in `tim-kerja`.
  - Recompiled via `docker compose run --rm gin-build` and verified all test suites passing with 100% success (`go test -v ./tests/e2e/...`).
- 95: Verified Database Stored Procedure Pagination & Total Row Integration for Reports and Lists:
  - Validated `/v1/umats/report` (`SP_BUS_RPT_UMAT`) returning paginated rows with `meta.page`, `meta.limit`, and `meta.total` (`total = 19503`).
  - Validated `/v1/donasi-sxy/report` (`SP_SXY_RPT_TRANSAKSI`) returning paginated rows with `meta.page`, `meta.limit`, `meta.total` (`total = 45239`), and `meta.total_jumlah`.
  - Rebuilt and restarted the Docker `gin_app` container (`docker compose up -d --build gin`) with the updated binary.
  - Updated E2E acceptance tests in `gin/tests/e2e/` (`master_data_e2e_test.go` and `transactions_e2e_test.go`) to explicitly assert `meta.total > 0` on list and report endpoints (`/v1/umats`, `/v1/umats/report`, `/v1/kelas`, `/v1/donasi-sxy`, and `/v1/donasi-sxy/report`).
- 96: Remediated CWE-434 File Upload / Script Injection via Polyglot Files in `UmatService` ([umat_service.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/umat_service.go)) and IIS `web.config` ([web.config](file:///Users/xicond/Workspace/www/GuangJiApps/gin/dist/web.config)):
  - Removed `image/gif` decoder registration and enforced strict MIME type validation (`image/jpeg` or `image/png` only), immediately rejecting GIF polyglot payloads (e.g. `GIF89a<?php ... ?>`).
  - Added strict file extension whitelisting (`.jpg`, `.jpeg`, `.png`) and blocked dangerous executable/script extensions (`.php`, `.phtml`, `.asp`, `.aspx`, `.jsp`, `.exe`, etc.) across all segments of the filename to prevent double extensions (e.g. `shell.php.jpg`).
  - Replaced shallow header checks with full in-memory image decoding (`image.Decode`) and re-encoding (`jpeg.Encode` / `png.Encode`) on disk save in `processAndSaveUmatFoto`. This completely strips all embedded script tags, PHP code, malicious EXIF metadata, and trailing bytes from the uploaded payload, guaranteeing the saved file contains only genuine compressed pixel data.
  - Enforced canonical extension (`.jpg` or `.png`) on disk based strictly on the decoded image format, preventing user-supplied `.php` extensions from reaching disk.
  - Added IIS `requestFiltering` `<fileExtensions>` denying `.php`, `.phtml`, `.asp`, `.aspx`, `.jsp`, `.cgi`, and `.exe` execution in `web.config`.
  - Added unit test suite `TestValidateImageHeader_Security` in `umat_service_test.go` covering polyglot GIF payloads, fake extensions, double extensions, and valid JPEG/PNG files. Verified with `go test -v ./internal/service` (**PASS**) and `docker compose run --rm gin-build` (**PASS**).
- 97: Added Graceful Shutdown implementation to `main.go` ([main.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/cmd/server/main.go)):
  - Launched HTTP server in an un-blocking goroutine with `srv.ListenAndServe()` and handled non-shutdown errors via `errors.Is(err, http.ErrServerClosed)`.
  - Added OS signal listener (`signal.Notify`) listening for `SIGINT`, `SIGTERM`, and `os.Interrupt`.
  - Configured graceful HTTP server shutdown (`srv.Shutdown(ctx)`) with a 10-second context timeout to allow active requests to finish cleanly before exiting.
  - Added background ticker cleanup (`middleware.GetFail2Ban().Stop()`), SQL database connection pool cleanup (`sqlDB.Close()`), and Redis client cleanup (`CloseRedisClient()` in `redis.go` ([redis.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/database/redis.go))).
  - Verified with `CGO_ENABLED=0 go build -o server ./cmd/server` (**PASS**).

## Next recommended steps
- Replace the placeholder authentication service with SQL Server-backed authentication and stored procedure integration.
- Add JWT/refresh-token handling and proper middleware.


