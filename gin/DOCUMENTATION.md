# Server-side implementation notes

## Completed
- Created a Gin-based server scaffold under the Gin project folder.
- Added a basic router with public endpoints: `/ping`, `/login`, `/forgot-password`, and `/reset-password`.
- Added environment-based configuration loading with `.env.example`.
- Added initial unit tests for the router and a Dockerfile for containerized builds.
- Verified the server starts and `/ping` returns `{"message":"pong"}`.
- Added `ChangePassword` method to `AuthService` and registered `/change-password` and `/v1/change-password` endpoints in Gin router.
- Updated `KelasService` `Create` and `Update` methods to follow the `domain.Kelas` GORM model (`T_TRX_KELAS`) and added lookup validations for `KodeKelas` (`B_KELASKHUSUS`) and `KodeFotang` (`B_FOTHANG`).
- Updated `router.go` error handling (`respondError`) to return 404 Not Found for missing records instead of 500.
- Added `GET /v1/kelas/:id/peserta` endpoint with stored procedure `SP_TRX_KELAS_PESERTA_SEARCH_DATA` and pagination (`page`, `limit`).
- Updated Vue frontend `KelasForm`, `KelasEdit`, `KelasView`, and `KelasPesertaTable` with pagination controls and preloaded lookup support.
- Fixed `IntBool` custom type scanning in `legacy_models.go` to support all database column data types (`bool`, `uint8`, `int16`, `int8`, `uint`, `string`, `float`, `[]byte`) and added JSON marshal/unmarshal handling, preventing row scan errors and empty response arrays.
- Updated `KelasService.Peserta` stored procedure invocation to use explicit named parameters (`@PageSize`, `@CurrentPage`, `@SortDirection`, `@TrxId`, `@FotangId`) with numeric `TrxId` parsing for `SP_TRX_KELAS_PESERTA_SEARCH_DATA`.
- Implemented full CRUD operations (`Create`, `Get`, `Update`, `Delete`) in `KelasPesertaService` (`T_TRX_KELAS_PESERTA`), registered router endpoints (`/kelas-peserta`), added unit tests (`kelas_peserta_service_test.go`), and updated Postman collection.
- Updated `Kelas` struct in `legacy_models.go` to add `omitempty` tag (`json:"trx_id,omitempty"`).
- Added GORM relation link (`Kelas *Kelas`) on `TrxId` in all child structs (`KelasPeserta`, `KelasPengabdi`, `KelasTopik`, `KelasKendaraan`, `KelasDonasi`, `KelasDonasiBarang`, `KelasPengeluaran`, `KelasMusik`, `KelasAbsensi`) in `legacy_models.go`.
- Implemented full CRUD services, unit tests, router endpoints, and Postman collections for all 8 Kelas sub-models (`KelasPengabdi`, `KelasTopik`, `KelasKendaraan`, `KelasDonasi`, `KelasDonasiBarang`, `KelasPengeluaran`, `KelasMusik`, `KelasAbsensi`).
- Converted `KelasPesertaTable.vue` to accordion panel layout with lazy fetching (only fetches data when expanded) and added popup dialog mode for creating and editing `Kelas Peserta` records.
- Added accordion table components with lazy fetching and popup dialog CRUD (Add, Edit, Delete) for `KelasPengabdiTable.vue`, `KelasTopikTable.vue`, `KelasDonasiTable.vue`, and `KelasDonasiBarangTable.vue`, integrated into `KelasEdit.vue` and `KelasView.vue`.
- Updated all `Kelas` sub-resource routes to nested `/v1/kelas/:id/<sub-resource>` URL path structure across Gin router (`router.go`), router unit tests (`router_test.go`), Postman collection (`apps-gin.postman_collection.json`), and Vue API client (`vue/src/api/kelas.ts`), and verified with `go test ./...`.
- Added Logistik/presence form fields (`anak`, `suster`, `menginap`, `makanan_pagi`, `makanan_siang`, `makanan_malam`) to `KelasPengabdiTable.vue` dialog form, state, and create/update handlers.
- Fixed stored procedure parameter placeholder count (from 8 to 9 placeholders) and `@LoginId` binding for `SP_TRX_KELAS_SEARCH_DATA` in `KelasService.List`, resolving data filtering truncation issue.
- Consolidated repetitive `T_APP_LOOKUP` queries into a unified `Lookup(db, categoryID, page, limit, filters)` function in `lookup_service.go`, supporting `limit=0` to return all records without pagination, replaced duplicated lookup code across `FotangService`, `KelasService`, and `TimKerjaService` (`Lookup`, `LookupSub`, `LookupReport`), updated `parsePaginationAndFilters` in `router.go` to preserve `limit=0`, and added unit tests in `lookup_service_test.go`, `fotang_service_test.go`, `kelas_service_test.go`, and `tim_kerja_service_test.go`.
- Created all dedicated lookup routes (`/v1/lookup/waktu-ciu-tao`, `/v1/lookup/gender`, `/v1/lookup/tcs`, `/v1/lookup/fotang`, `/v1/lookup/kelas`, `/v1/lookup/pendidikan`, `/v1/lookup/kelas-umum`, `/v1/lookup/pekerjaan`, `/v1/lookup/keluarga`, `/v1/lookup/status`, and dynamic `/v1/lookup/category/:category`) in Gin router (`router.go`), updated Postman collection (`apps-gin.postman_collection.json`), and added unit tests in `lookup_service_test.go` and `router_test.go`.
- Updated `Lookup` function and `LookupService.Lookup` in `lookup_service.go` to support optional `customSubQuery` parameter (accepting either a custom `*gorm.DB` query or a modifier callback function `func(*gorm.DB) *gorm.DB`) to override or append conditions to the default `T_APP_LOOKUPCATEGORY` subquery. Verified with unit tests in `lookup_service_test.go`.
- Registered `/v1/tim-kerja/lookup`, `/v1/tim-kerja/lookup/:id/sub`, and `/v1/tim-kerja/lookup-report` endpoints in Gin router (`router.go`), added Postman collection entries (`apps-gin.postman_collection.json`), and verified with unit tests in `router_test.go` and `tim_kerja_service_test.go`.
- Optimized backend lookup validation in `UmatService` (`umat_service.go`) to execute lookup checks concurrently using goroutines (`sync.WaitGroup`), collecting all field validation errors into an aggregated array before returning, minimizing GC overhead and query latency. Verified with `go test`.
- Standardized HTTP 400 validation error handling across `router.go`, `validator.go`, and `umat_service.go` to return `{ "error": "Validation failed", "details": { "fieldname": ["error message"] } }` structure for struct binding, `ValidateStruct`, and concurrent lookup validation (`validateUmatLookups`). Verified with `go test ./...`.
- Configured `validator.New()` with `RegisterTagNameFunc` in `validator.go` to ensure validation error detail keys (`details map[string][]string`) use the JSON tag names (`nama_indonesia`, `status_umat`, `jenis_kelamin`, etc.) instead of Go struct field names. Verified with `go test`.
- Created reusable `<FieldErrors />` component (FieldErrors.vue) and integrated field error details rendering into UmatForm.vue, UmatCreateList.vue, and UmatEditList.vue to display multiple error rows directly beneath each related input field based on backend `details` JSON map. Verified with `vue-tsc --noEmit`.
- Integrated `<FieldErrors />` component and field validation error handling across all Kelas components (KelasForm.vue, KelasPesertaTable.vue, KelasPengabdiTable.vue, KelasTopikTable.vue, KelasDonasiTable.vue, and KelasDonasiBarangTable.vue) as well as views (KelasCreate.vue and KelasEdit.vue) to display field error messages in multiple rows under input fields and dialog forms based on backend `details`. Verified with `vue-tsc --noEmit`.
- Updated `UmatForm.vue` with Tim Kerja lookup select (`getLookupTimKerja`), Pekerjaan lookup select (`getLookupPekerjaan`), Ikrar 1-6 checkboxes (`ikrar_1`..`ikrar_6`), status checkboxes and paired DatePickers (`ren_chai_pan`, `lien_ciang_pan`, `ciang_yen_pan`, `ching_khou`, `meninggal`), `nama_tcs_lain`, `nama_fotang_lain`, and numeric `uang_pahala`. Verified with `vue-tsc --noEmit` and `go test ./...`.
- Registered `GET /v1/topics` endpoint in Gin router (`router.go`), added `getTopikLookup` method in `topic.ts`, and updated `KelasTopikTable.vue` to use `<LookupSelect>` for Nama Topik lookup, displaying the selected Topic's Kategori (`topic_category`) and Keterangan (`description`) as static text in `el-form-item`. Verified with `vue-tsc --noEmit` and `go test ./...`.
- Updated `KelasForm.vue` to use `<LookupSelect>` with `lookupApi.getLookupKelasLevel` (`/v1/lookup/kelas-level`) for "Tingkat / Level". Added `page`, `limit`, and `lookup_description` query parameters across all 17 lookup endpoints in [apps-gin.postman_collection.json](file:///Users/xicond/Workspace/www/GuangJiApps/gin/postman/apps-gin.postman_collection.json). Verified with `vue-tsc --noEmit` and `go test ./...`.
- Updated `KelasPengabdiTable.vue` to render Data Logistik / Kehadiran as a dynamic per-day table multiplied by the class duration (`Tanggal Selesai` - `Tanggal Mulai`), supporting numeric inputs for `anak` & `suster` and checkboxes for `menginap`, `makanan_pagi`, `makanan_siang`, & `makanan_malam`. Form values are parsed from comma-separated strings on load and saved as comma-separated values on submit. Verified with `vue-tsc --noEmit` and `go test ./...`.
- Updated `KelasPesertaTable.vue` to render Data Logistik / Kehadiran as a dynamic per-day table multiplied by the class duration (`Tanggal Selesai` - `Tanggal Mulai`), supporting numeric inputs for `anak` & `suster` and checkboxes for `menginap`, `makanan_pagi`, `makanan_siang`, & `makanan_malam`. Form values are parsed from comma-separated strings on load and saved as comma-separated values on submit. Verified with `vue-tsc --noEmit` and `go test ./...`.
- Updated `KelasForm.vue` to add proper padding to `.mc-collapse` header (`0.75rem 1rem`) and body (`1.25rem 1.25rem 0.5rem 1.25rem`) for the Pembawa Acara / MC collapsible section. Verified with `vue-tsc --noEmit`.
- Updated `StartDate`, `EndDate`, and `Deadline` fields in `Kelas` struct ([legacy_models.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/domain/legacy_models.go#L638)) from `*time.Time` to `*DateOnly` so that GET `/v1/kelas/:id` returns dates in ISO `YYYY-MM-DD` format and POST/PATCH `/v1/kelas` handles date strings (`2019-04-14T00:00:00Z`, `2019-04-14`, or empty `""` / `null`) cleanly without JSON parsing errors. Registered `DateOnly` in `validator.go` for date range comparison. Verified with `vue-tsc --noEmit` and `go test ./...`.
- Added `FieldTranslations` dictionary mapping and `GetFieldLabel` / `FormatFieldName` helper functions in `legacy_models.go` to translate struct field names (e.g. `Umat.NamaIndonesia` into `Nama Indonesia`), falling back to space-separated PascalCase/snake_case formatting by default, and updated `FormatValidatorErrors` in `validator.go` to present user-friendly error messages while preserving original detail keys for frontend form binding. Verified with unit tests in `legacy_models_test.go` and `validator_test.go`.
- Refactored `validateKelasLookups` in `kelas_service.go` to validate lookup fields (`kode_kelas`, `kode_fotang`, `level`) concurrently using `sync.WaitGroup` and return `&ValidationError{Details: details}` matching `validateUmatLookups` in `umat_service.go`. Updated `KelasService.Update` to use `validateKelasLookups`. Verified with `TestKelasService`.
- Added `validatePesertaLookups` (`trx_id` from `Kelas`, `id_peserta` from `Umat`, `tim_kerja` from `B_TIMKERJA`), `validatePengabdiLookups` (`trx_id` from `Kelas`, `id_pengabdi` from `Umat`, `tim_kerja` from `B_TIMKERJA`, `sub_kerja` from `B_SUBKERJA`), and `validateTopikLookups` (`trx_id` from `Kelas`, `kode_topik` from `Topic`, `penceramah` from `Umat`) across `kelas_peserta_service.go`, `kelas_pengabdi_service.go`, and `kelas_topik_service.go`, returning structured `&ValidationError{Details: details}`. Verified with `go test ./...`.
- Updated `KelasForm.vue` to use a unified `el-date-picker` `type="daterange"` component bound via a computed property to `formData.start_date` and `formData.end_date`. Added `validate:"omitempty,gtefield=StartDate"` to `EndDate` in `legacy_models.go` and `gtefield`/`ltefield` error message translations in `validator.go` to enforce `Kelas.StartDate <= Kelas.EndDate`. Verified with `vue-tsc --noEmit` and `go test ./...`.
- Implemented `KelasService.Report` method and Gin endpoints (`GET /v1/kelas/:id/report`) to stream SSRS Excel reports (`rpt_trx_kelas`) with zero-copy buffer pooling (`sync.Pool`), configurable base URL (`REPORT_SERVER_URL`), username & password authentication (`REPORT_SERVER_USERNAME`, `REPORT_SERVER_PASSWORD`), HTTP Basic Auth, and HTTP Digest Auth challenge retry. Retrieved default `SubWhId` automatically from `T_WH_USER_MATRIX_MST` (`AdminMatrix`) linked to logged in `userID`. Installed `excelize/v2`, added Postman entry, and added unit tests in `kelas_service_test.go`. Verified with `go test ./...`.
- Added `downloadReport` method to `kelasApi` (`vue/src/api/kelas.ts`) and added a green Download action button (`Download` icon) with `ElLoading.service` full-page overlay loading, automatic Blob JSON error parsing (`err.response.data.text()`), and cleanup handling on success/failure in `KelasList.vue` (`vue/src/views/transaction/KelasList.vue`). Verified with `npx vue-tsc --noEmit`.
- Added search by `lookup_value` and multi-field keyword search (`LookupDescription`, `LookupValue`, `LookupId`) across `Lookup` in `lookup_service.go`, `FotangService.Lookup`, `KelasService.Lookup`, `TimKerjaService.Lookup`, `TimKerjaService.LookupSub`, `TimKerjaService.LookupReport`, and `TimKerjaService.List` in `tim_kerja_service.go`. Updated all 17 lookup requests in `apps-gin.postman_collection.json` with `lookup_value` and `search` query parameters, added unit tests in `lookup_service_test.go`, and verified with `go test ./...`.
- Exported `lookup_value`, `lookup_id`, and `search` query parameters in `types/lookup.ts` ([lookup.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/types/lookup.ts)) and updated API methods in `api/fotang.ts` ([fotang.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/api/fotang.ts)), `api/kelas.ts` ([kelas.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/api/kelas.ts)), and `api/lookup.ts` ([lookup.ts](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/api/lookup.ts)). Added `checkAndFetchMissingSelectedValue()` in `LookupSelect.vue` ([LookupSelect.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/components/common/LookupSelect.vue)) to auto-fetch missing selected options by `lookup_value`/`lookup_id` when loading edit forms where the selected item is on page 2+. Verified with `npx vue-tsc --noEmit`.
- Implemented paginated `Report` (`SP_SXY_RPT_TRANSAKSI`) returning `SxyDonasiReport` & total summary, and `ReportExcel` SSRS Excel streaming in `donasi_sxy_service.go` ([donasi_sxy_service.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/service/donasi_sxy_service.go)). Registered `GET /v1/donasi-sxy/report` and `GET /v1/donasi-sxy/report/excel` endpoints in `router.go` ([router.go](file:///Users/xicond/Workspace/www/GuangJiApps/gin/internal/api/router.go)) and added requests to `apps-gin.postman_collection.json` ([apps-gin.postman_collection.json](file:///Users/xicond/Workspace/www/GuangJiApps/gin/postman/apps-gin.postman_collection.json)). Updated `SxyReportList.vue` ([SxyReportList.vue](file:///Users/xicond/Workspace/www/GuangJiApps/vue/src/views/report/SxyReportList.vue)) with filters (Donatur, Penggalang, Fotang `LookupSelect`, DateRange), data table pagination, table footer summary (`show-summary` & `:summary-method="getSummaries"`) right-aligned with the `Jumlah` column, total summary banner, and conditional Excel report download (`ReportExcel`) enabled when rows exist. Verified with `go test ./...` and `npx vue-tsc --noEmit`.



## Next recommended steps
- Replace the placeholder authentication service with SQL Server-backed authentication and stored procedure integration.
- Add JWT/refresh-token handling and proper middleware.
- Add structured logging, validation, and more comprehensive API tests.
- Add Docker Compose integration for the Gin service alongside the existing MSSQL container.
